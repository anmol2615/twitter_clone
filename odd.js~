'use strict';
(
    function findOdd(arr)
    {
        console.log('odd numbers are: \n');
        for(var i=0;i<arr.length;i++)
        {
            if(arr[i] & 1)
            console.log(arr[i]+' at position '+i);
        }
    }
)([1,3,5,2,7,9]);